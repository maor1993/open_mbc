# Open Mbc

Open Multiband compressor! for all your multiband compression needs


## Building

After installing [Rust](https://rustup.rs/), you can compile Open Mbc as follows:

```shell
cargo xtask bundle open_mbc --release
```
