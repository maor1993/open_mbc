{
    "configurations": [
        {
            "type": "lldb",
            "request": "attach",
            "name": "Attach by PID",
            "pid": "${command:pickProcess}", // Prompts to select a running process
            "sourceLanguages": [
                "rust"
            ],
        },
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/<program>",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}